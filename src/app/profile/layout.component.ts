import { Component } from '@angular/core'; // Import the Component decorator from Angular core 
 
@Component({ templateUrl: 'layout.component.html' }) //  Define the component and specify the path to its template
export class LayoutComponent { } //  Declare the LayoutComponent class, which will be used as a layout container for other components
 
 