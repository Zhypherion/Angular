export const environment = {
    production: true,
    // Setting the environment to production mode, indicating that this configuration is for the production build of the app

    apiUrl: 'http://localhost:4000'
    // Defining the base URL for the API the app will interact with, in this case, a local server running on port 4000
};
